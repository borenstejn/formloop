# Question types reference

Stub — to flesh out in Phase 3.

For now, the canonical type definitions live in [`../apps/server/lib/types.ts`](../apps/server/lib/types.ts) and the spec format is documented in the skill at `~/.claude/skills/ask-form/SKILL.md` (Jérôme's local install).
